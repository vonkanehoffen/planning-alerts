import { createApolloFetch } from 'apollo-fetch';
import { INSERT_PLANNING_APP, UPDATE_PLANNING_APP } from '../queries';

/**
 * Store planning application in Hasura back end
 * @param app -  should follow planning_app_insert_input format in schema.
 */
export function storePlanningApp(app) {

}